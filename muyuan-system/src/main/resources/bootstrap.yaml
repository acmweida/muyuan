spring:
  application:
    name: system
  main:
    allow-bean-definition-overriding: true
server:
  port: 20003
logging:
  config: classpath:log4j-${spring.profiles.active}.xml
  level:
    com.muyuan.system.infrastructure.persistence.mapper.*: debug
    com.alibaba.nacos.client.*: warn
    com.alibaba.cloud.dubbo.*: error
    org.apache.dubbo.*: error
    io.lettuce: error


mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    lazy-loading-enabled: true
  mapper-locations: classpath*:mapper/*.xml

dubbo:
  scan:
    base-packages: com.muyuan.system.interfaces.facade.api
  registry:
    address: spring-cloud://127.0.0.1
  protocol:
    name: dubbo
    port: 21003
  application:
    name: ${spring.application.name}
    id: ${spring.application.name}
  consumer:
    check: false

gen:
  packageName: com.muyuan
  autoRemovePre: true
  tablePrefix: t_

db:
  read-mehod-prefix: select,find,get
  write-mehod-prefix: insert,update,delete,del
