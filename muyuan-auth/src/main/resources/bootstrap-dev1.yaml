spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1
      discovery:
        server-addr: 127.0.0.1
  application:
    name: auth
  redis:
    host: 127.0.0.1
server:
  port: 18790
management:
  endpoints:
    web:
      exposure:
        include: "*"
